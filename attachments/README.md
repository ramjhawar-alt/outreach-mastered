# Attachments

Drop any PDF here (e.g. your resume) to attach it to every outreach email.

Then set the path in your `.env`:

```
OUTREACH_RESUME_PDF=attachments/YourResume.pdf
```

The file will be attached to every email sent via `--email` or `--email-daily`.
